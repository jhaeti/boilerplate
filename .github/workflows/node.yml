name: FRONTEND CI

on:
    push:
        branches: [ main ]

    workflow_dispatch:
    
jobs:
    Next-build:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                node-version: [16.x] # Using node version 16
        steps:
            - name: Checkout
              uses: actions/checkout@v2

